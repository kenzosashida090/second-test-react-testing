name: Output information
on: workflow_call # this trigger will allow us to call this workflow in another workflow
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output Github context
        run: echo "${{ toJSON(github) }}"
