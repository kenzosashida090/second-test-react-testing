import { it, expect, describe } from 'vitest'

import {faker} from '@faker-js/faker'
import { db } from './db'
describe('group', ()=>{
    it("it sh ould be true",async ()=>{
        const product = db.product.create() // create a random product from the db.tshja       
      
    
    })
})
